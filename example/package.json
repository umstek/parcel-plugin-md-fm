{
  "name": "example",
  "version": "0.8.0",
  "private": true,
  "description": "Markdown loading example",
  "license": "MIT",
  "author": "Wickramaranga Abeygunawardhana <umstek@live.com> (http://www.umstek.tk)",
  "scripts": {
    "build": "parcel build src/index.html --dist-dir dist --public-url ./ --no-cache",
    "start": "parcel src/index.html --no-cache",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "parcel": "^2.16.4",
    "parcel-transformer-md-fm": "workspace:*"
  }
}
